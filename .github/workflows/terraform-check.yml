name: Terraform Check

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  verify:
    name: verify (no AWS credentials)
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Read pinned versions
        id: versions
        shell: bash
        run: |
          echo "terraform=$(cat .terraform-version)" >> "$GITHUB_OUTPUT"
          echo "tflint=v$(cat .tflint-version)" >> "$GITHUB_OUTPUT"
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: ${{ steps.versions.outputs.terraform }}
          terraform_wrapper: false
      - uses: terraform-linters/setup-tflint@90f302c255ef959cbfb4bd10581afecdb7ece3e6 # v4.1.1
        with:
          tflint_version: ${{ steps.versions.outputs.tflint }}
      - name: Install checksum-verified OPA
        run: bash scripts/install-opa-ci.sh
      - name: Fmt, validate, mock tests, lint, regression tests and policy
        run: make verify
      - name: Verify completion evidence and pre-push connection
        shell: bash
        run: |
          make harness-status
          head_sha="$(git rev-parse HEAD)"
          printf 'refs/heads/ci %s refs/heads/ci %040d\n' "$head_sha" 0 | .githooks/pre-push
