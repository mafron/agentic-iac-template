# Provision this backend separately; never bootstrap it with this workload state.
bucket                = "replace-with-prod-state-bucket"
key                   = "harness/prod/terraform.tfstate"
region                = "ap-northeast-1"
encrypt               = true
use_lockfile          = true
expected_bucket_owner = "111111111111"
# No access keys here. Use the current short-lived session.
